import { ExerciseShortcuts } from './exercise-shortcuts';

describe('ExerciseShortcuts', () => {
  it('should create an instance', () => {
    const directive = new ExerciseShortcuts();
    expect(directive).toBeTruthy();
  });
});
